<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- Interceptor 설정 -->
	<!-- Interceptor 객체 생성 -->
	<!-- 객체를 생성하고 참조하는 방법일 경우 -->
	<beans:bean class="kr.co.practice.interceptor.TestInterceptor" id="testInterceptor"></beans:bean>
	
	<!-- URL - Interceptor 매핑 -->
	<interceptors>
		<interceptor>
			<!-- URL 경로 -->
			<!-- mapping beans 순서 변경하면 안됨 -->
			<mapping path="/**"/> <!-- root로 시작하는 모든 경로 -->
			<beans:ref bean="testInterceptor"/> <!-- Interceptor 지정 -->
		</interceptor>
		
		<interceptor>
			<mapping path="/qna/*"/>
			<mapping path="/members/myPage"/>
			<!-- 제외할 URL -->
			<exclude-mapping path="/qna/list.iu"/>
			<!-- 즉석에서 생성하여 사용하는 경우, 여러군데에서 사용 불가 -->
			<beans:bean class="kr.co.practice.interceptor.MemberInterceptor" id="memberInterceptor"></beans:bean>
		</interceptor>
		<!-- URL 매핑이 겹치면 겹치는 Interceptor 모두 실행(선언된 순서대로) -->
		<!-- myPage로 가면 testInterceptor 실행 후 true 리턴 시 memberInterceptor 실행 -->
		
		<interceptor>
			<mapping path="/qna/update.iu"/>
			<mapping path="/notice/update.iu"/>
			<beans:bean class="kr.co.practice.interceptor.WriterCheckInterceptor" id="writerCheckInterceptor"></beans:bean>
		</interceptor>
		
	</interceptors>
	
	<context:component-scan base-package="kr.co.practice" />
	
	
	
</beans:beans>
