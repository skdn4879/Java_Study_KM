<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		    
<mapper namespace="kr.co.practice.board.qna.QnaDAO">
	
	<select id="getList" resultType="QnaDTO">
		SELECT * FROM qna ORDER BY regdate DESC
	</select>
	
	<select id="getDetail" parameterType="BoardDTO" resultType="QnaDTO">
		SELECT * FROM qna WHERE num = #{num}
	</select>
	
	<insert id="setAdd" parameterType="BoardDTO">
		INSERT INTO qna(title, contents, writer, ref, step, depth) VALUES(#{title}, #{contents}, #{writer}, 0, 0, 0)
	</insert>
	
	<update id="setUpdate" parameterType="BoardDTO">
		UPDATE qna SET title = #{title}, contents = #{contents} WHERE num = #{num}
	</update>
	
	<delete id="setDelete" parameterType="BoardDTO">
		DELETE FROM qna WHERE num = #{num}
	</delete>
	
</mapper>